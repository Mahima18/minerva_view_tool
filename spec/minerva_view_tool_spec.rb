require "spec_helper"

describe MinervaViewTool do
  it "has a version number" do
    expect(MinervaViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
